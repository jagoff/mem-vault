"""Browser UI for mem-vault.

Lightweight FastAPI + HTMX app to browse, search, edit, and delete memories
without leaving the terminal. Pure server-rendered, zero build step, zero
JavaScript framework — just HTMX for partial updates.

Boot it with:

    mem-vault ui [--host 127.0.0.1] [--port 7880]
"""
